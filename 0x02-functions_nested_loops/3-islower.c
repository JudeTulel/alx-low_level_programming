#include "main.h"
/**
  *_islower in main.h
  *Using _putchar to print
  *c is the input
  *Return 1 lowercase 
  *Return 0 uppercase
*/
int _islower(int c)
{
	char i;
	int lower = 0;
	
	for (i = 'a';i <= 'z'; i++)
	{
		if (i == c)
			lower = 1
	}
	
	return (lower)
}
