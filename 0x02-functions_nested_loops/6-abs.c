#include "main.h"
/**
 *_abs(Int r) in main.h
 *r is input
 * to Return abs val of r >= 0 Return(r)
 * to Return abs val of r<0 Return(r*-1)
*/
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
} 
